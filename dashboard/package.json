{
  "name": "pigproject-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-dashboard-data.mjs",
    "dev": "npm run generate:data && vite",
    "build": "npm run generate:data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pretendard": "^1.3.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^6.4.3"
  }
}
